[package]
name = "mobile"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
dioxus = { workspace = true, features = ["router"] }
ui = { workspace = true }

[features]
default = ["mobile"]
mobile = ["dioxus/mobile"]

